fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
mss>=9.0.1
Pillow>=10.0.0
pyautogui>=0.9.54
numpy>=1.24.0
aiortc>=1.9.0
pydantic>=2.0
loguru>=0.7.0
# macOS Accessibility API (AXUIElement) — 窗口焦点精确切换
pyobjc-framework-ApplicationServices>=10.0; sys_platform == "darwin"
# AI Agent — 纯 HTTP 调用 Anthropic Messages API，不依赖 SDK
# anthropic>=0.39.0  # 不需要，已用 urllib 直接调用
# MCP Server — 供外部工具调用
mcp>=1.0.0
